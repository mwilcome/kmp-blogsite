Run `npm start` 

The script for running start will:
1) Pull in the KMP project's shared-logic code into the vendor/shared-logic folder
2) go back and serve the angular app automatically


Thats it! Should be running on localhost:4200